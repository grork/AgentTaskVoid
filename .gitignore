# Build outputs
bin/
obj/

# Phase 12 release packaging outputs (publish/, msix/, and the throwaway dev
# signing cert) -- build/Atv.Release.targets; never checked in.
artifacts/

# Phase 14 host-event recorder: raw captures may carry prompts, cwd paths,
# tool inputs -- possibly secrets. Only distilled findings (docs/host-events/)
# are ever committed.
tools/host-event-recorder/captures/

# User-specific files
*.user
*.suo
.vs/

# Claude Code local settings (user-specific overrides)
.claude/settings.local.json

# Rider
.idea/
